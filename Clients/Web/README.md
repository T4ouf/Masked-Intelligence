# Web frontend for Masked-Intelligence

## Dependencies
Only `vite` at the moment.
To install, npm packages, run
```sh
npm install
```

## Running the web client

To startup the frontend with vite, run
```sh
npm run dev
```

## TODO
Use flexbox to align image and prompt text.
